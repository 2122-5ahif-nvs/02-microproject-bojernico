# Configuration file
# key = value
quarkus.swagger-ui.always-include=true

# datasource configuration
quarkus.datasource.db-kind = postgresql
quarkus.datasource.username = app
quarkus.datasource.password = app
quarkus.datasource.jdbc.url = jdbc:postgresql://localhost:5432/db

# drop and create the database at startup (use `update` to only update the schema)
quarkus.hibernate-orm.database.generation=drop-and-create

# Quarkus - Instance
quarkus.package.type=uber-jar

# Swagger-UI

mp.openapi.extensions.smallrye.info.title=Kochrezepte
mp.openapi.extensions.smallrye.info.version=1.1
mp.openapi.extensions.smallrye.info.description=Manage recipes, ingredients and create your own meals (manage them as well) 
mp.openapi.extensions.smallrye.info.contact.name=@bojernico
mp.openapi.extensions.smallrye.info.contact.url=https://gutefrage.net/